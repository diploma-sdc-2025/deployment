services:
  swagger-ui:
    image: swaggerapi/swagger-ui:latest
    ports:
      - "8088:8080"
    environment:
      SWAGGER_JSON: /docs/openapi.yaml
    volumes:
      - ../api-docs/reference:/docs:ro
